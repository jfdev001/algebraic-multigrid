name: Cmake Doxygen

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
    # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
    BUILD_TYPE: Release
    DOCS_DIR: docs
    
jobs:
  build-docs:
    # The CMake configure and build commands are platform agnostic and should work equally well on Windows or Mac.
    # You can convert this to a matrix build if you need cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    runs-on: ubuntu-latest

    # Allow write to gh-pages https://github.com/peaceiris/actions-gh-pages?tab=readme-ov-file
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3
    # https://github.com/marketplace/actions/doxygen-install
    - uses: ssciwr/doxygen-install@v1
    
    # https://github.com/marketplace/actions/setup-eigen3
    - name: Install Eigen3
      uses: kupns-aka-kupa/setup-eigen3@v1
      with:
        version: 3.4.0

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build
      # Build your program with the given configuration
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}

    - name: Doxygen documentation generation
      working-directory: ${{github.workspace}}/build
      run: make docs

    - name: Deploy Documentation
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ${{github.workspace}}/build/${{env.DOCS_DIR}}/html